<!DOCTYPE html>
<html>
    <head>
        <title>종목토론방</title>
        <meta charset="utf-8" />
        <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
        <style>
            span { 
                display: inline-block;
                padding: 10px;
                width: 90px; 
            }
        </style>
    </head>
    <body>
        <h1>아이디찾기</h1>
        <div>
            <span>이름</span><input id='name-input' type='text'>
            <br>
            <span>핸드폰번호</span><input id='phone-input' type='text'>
            <br>
            <button id='ok-Btn' onclick="getUserId()">확인</button>
            <button id='home-Btn' onclick="location.href='/'">로그인화면</button>
            <button id='findPw-Btn' onclick="location.href='/findPw'">비밀번호찾기</button>
        </div>            
    </body>
</html>

<script>
    function getUserId() {
        const name = $('#name-input').val();
        const phone = $('#phone-input').val();
        $.ajax({
            url: '/request-find-user-id',
            type: 'POST',
            data: {
                name: name,
                phone: phone,
            },
            success: function(data) {
                if(data == '') {
                    alert('정보를 확인해 주세요');
                } else {
                    const result = JSON.parse(data);
                    alert(`아이디는 ${result.id} 입니다`);
                }
            }
        })
    }
</script>